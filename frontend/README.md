# VIT Result Frontend (React + Vite)

## Prereqs
- Node.js 18+

## Run
```bash
cd frontend
npm install
npm run dev
```
Site: http://localhost:5173

Make sure the backend (Spring Boot) is running at http://localhost:8080
